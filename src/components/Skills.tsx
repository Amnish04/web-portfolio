import Layout from "./Layout";

const Skills = () => {
    return (
        <Layout id="skills">
            <h2 className="font-headings font-semibold text-3xl">Skills</h2>
        </Layout>
    );
};

export default Skills;
